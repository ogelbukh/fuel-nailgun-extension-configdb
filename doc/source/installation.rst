============
Installation
============

At the command line::

    $ pip install fuel-nailgun-extension-configdb

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fuel-nailgun-extension-configdb
    $ pip install fuel-nailgun-extension-configdb
